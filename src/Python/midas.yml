# Image type can be either docker or singularity
Image type: "docker"

Base: "postgres"

Setup:
  6: "apt-get update"
  7: "apt-get install -y python3 gcc"
  8: "apt-get install -y curl"
  9: "apt-get install -y wget"

Contents:
  4: "sample-input.midas.json:/local/sample-input.midas.json"
  5: "sample-input.midas.json:/MIDAS/MIDAS/sample-input.midas.json"

Environmental variables:
  2:
    c1: test
    c2: test2
  3:
    d1: sample1
    d2: sample2
 
Default command: echo 'In the hopes that everything I touch, turns to gold...'
